# 鼠标飞控修复方案：俯仰奇异区与类自由视角（2026-04-17）

## 1. 问题定义
- 现象 A：鼠标上拉时机头接近地平 90 度后难以逾越，并出现明显左右乱滚。
- 现象 B：已接入“类自由视角”但体感不明显，视角仍像被机身强绑定。
- 现象 C：横向机动虽有滚转，但在极端俯仰区间容易被耦合噪声破坏可控性。

## 2. 根因结论（代码层）
- 根因 1（核心）：姿态更新链路使用 `MatrixToEuler()` 反解欧拉角，`asin` 使 pitch 自然落在 `[-90, 90]`，接近奇异区会触发 yaw/roll 耦合放大。
- 根因 2（视角）：类自由视角偏转主要叠加在 `updateCameraRotate`，但主视角最终仍由 `setAngles()` 对 `player.rotationYaw/Pitch` 覆写，导致偏转体感被抵消。
- 根因 3（控制分配）：奇异区附近仍沿用常规权重，roll/yaw/pitch 争抢控制权，出现“接近 90 度后滚转突增”。

## 3. 修复目标
- 目标 1：让“类自由视角”在鼠标飞控模式下主观可见且可控。
- 目标 2：在不做全量四元数重构前，先压制 90 度附近的发散与乱滚。
- 目标 3：保持现有手感基础，不回退已验证的滚转优先策略。

## 4. 实施策略（两阶段）

### 阶段 A：视角链路修正（低风险，优先）
- 思路：把类自由视角偏转接入主视角覆盖点，而不是仅接入 `updateCameraRotate`。
- 生效条件：`WT激活 && 非FreeLook && thirdPersonView==1 && PseudoFreeLook开关开启`。
- 预期效果：鼠标偏转时可明显侧看/抬看，且不触发 FreeLook 锁舵。

### 阶段 B：奇异区缓解（中风险，不重构）
- 思路：在 `|pitch|` 进入阈值区后动态切换控制权重。
- 关键策略：
  - `SingularityWeight`：例如 `|pitch|` 从 78 到 89 线性升高到 1。
  - 降 yaw：`yawCmd *= (1 - 0.65 * SingularityWeight)`。
  - 稳 roll：限制 rollErr 增益上限，避免瞬时冲击。
  - 降 pitch 突变：接近 90 度时抑制 pitch 一阶变化率，防止“顶死”后乱滚。
- 预期效果：接近 90 度时仍可控，不出现左右疯狂摆滚。

## 5. 相关代码改造清单（类/方法级）

### 5.1 `MCH_EntityAircraft.setAngles(...)`（主改）
- 文件：`src/main/java/mcheli/aircraft/MCH_EntityAircraft.java`
- 改动点：
  - 在 `player.rotationYaw/rotationPitch` 覆写分支叠加 pseudo freelook 偏转。
  - 仅对 `MCP_EntityPlane` 且条件满足时启用偏转。
  - 保持 FreeLook 逻辑不变（FreeLook 仍锁舵）。
- 伪代码：
```java
if (this instanceof MCP_EntityPlane) {
    MCP_EntityPlane p = (MCP_EntityPlane)this;
    if (p.isWTPseudoFreeLookCameraActive()) {
        yawToApply += p.getWTCameraYawOffset();
        pitchToApply += p.getWTCameraPitchOffset();
    }
}
```

### 5.2 `MCP_ClientPlaneTickHandler.onTick...`（配套）
- 文件：`src/main/java/mcheli/plane/MCP_ClientPlaneTickHandler.java`
- 改动点：
  - 可保留 `updateCameraRotate` 偏转叠加，但应避免与 `setAngles` 重复叠加。
  - 推荐：主视角偏转迁入 `setAngles` 后，这里仅传原始 `player.rotationYaw/Pitch`。

### 5.3 `MCP_EntityPlane.updateWTMouseAimControl(...)`（奇异区权重）
- 文件：`src/main/java/mcheli/plane/MCP_EntityPlane.java`
- 改动点：
  - 新增 `SingularityWeight`（基于当前 `abs(getRotPitch())`）。
  - 按权重重分配 `yaw/pitch/roll`：
    - 降 yaw 权重
    - 限制 rollErr 放大
    - 对 pitchCmd 做变化率限制（rate limit）
  - 日志新增字段：`singW`, `yawScale`, `rollScale`, `pitchRateLimit`.

## 6. 配置与开关建议
- 现有复用：
  - `MouseAimPlanePseudoFreeLookEnabled`
  - `MouseAimPlanePseudoFreeLookYawLimit`
  - `MouseAimPlanePseudoFreeLookPitchLimit`
- 建议新增（可后加）：
  - `MouseAimPlaneSingularityProtectEnabled`（默认 true）
  - `MouseAimPlaneSingularityStartPitch`（默认 78）
  - `MouseAimPlaneSingularityFullPitch`（默认 89）

## 7. 调试与验收

### 7.1 调试命令
- `/mcheli debug mouseaim true`
- `/mcheli debug mouseaim status`
- `/mcheli debug mouseaim snapshot`
- `/mcheli debug mouseaimprofile normal|aggressive`

### 7.2 验收场景
- 场景 A（视角）：第三人称后视，鼠标偏转时能明显侧看，但飞控不锁舵。
- 场景 B（奇异区）：持续上拉到高仰角，控制不发散，不出现无规律左右滚。
- 场景 C（回归）：中低仰角区手感不劣化，左右机动仍保留滚转优先。

## 8. 风险与回滚
- 风险 1：视角偏转重复叠加导致过度“甩镜头”。
  - 处置：只保留 `setAngles` 一处主叠加，另一路仅做辅助。
- 风险 2：奇异区保护过强导致“拉不起来”。
  - 处置：通过 `SingularityStart/Full` 与 `yaw/roll/pitch`缩放系数调参。
- 回滚策略：
  - 阶段 A 与阶段 B 分开提交；A 稳定后再上 B。

## 9. 结论
- 当前最优路径不是直接大改四元数，而是先修主视角链路，再加奇异区保护。
- 这条路径可以在保持现有飞控成果的前提下，显著改善“看不出自由视角”和“90 度附近乱滚”问题。
