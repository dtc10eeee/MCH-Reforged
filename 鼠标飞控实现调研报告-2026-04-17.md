# 鼠标飞控实现调研报告（以《战争雷霆》风格为参考）

## 1. 调研目标
- 梳理主流游戏中的“鼠标飞控（Mouse Flight Control）”实现思路。
- 重点分析《战争雷霆》类鼠标操控手感背后的控制链路。
- 给出可在本项目中落地的实现框架、参数起步值与调参顺序。

## 2. 主流实现范式

### 2.1 指向式（Mouse Aim）
- 核心思想：鼠标不直接控制舵面，而是控制“目标方向/目标姿态”。
- 飞控系统（稳定器/Instructor）根据误差自动输出副翼、升降舵、方向舵。
- 优点：上手快、瞄准稳定、对新手友好。
- 典型：偏街机或半模拟空战游戏（《战争雷霆》街机/历史模式常见此体验）。

### 2.2 角速度式（Rate Command）
- 核心思想：鼠标位移映射为俯仰/横滚/偏航角速度指令。
- 回中后通常进入“保持当前姿态”或“阻尼回正”。
- 优点：可控性高、可形成更细致机动风格。
- 缺点：对玩家飞行基础要求更高。

### 2.3 虚拟摇杆式（Virtual Stick）
- 核心思想：鼠标位置等价于摇杆偏转量，再由稳定器二次处理。
- 常配合输入曲线、死区、自动协调转弯等辅助。
- 优点：实现相对简单，且可保留一定“手动飞行”感。

## 3. 《战争雷霆》风格的关键特征（工程推断）
- 玩家层感知是“鼠标拖着机头走”，底层通常是“目标驱动 + 飞控闭环”。
- 控制对象主要是“目标方向/目标角速度”，不是舵面原始量。
- 典型存在 Instructor 保护策略：失速保护、攻角限制、过载限制、协调转弯。
- 在低速/大迎角条件下会主动抑制过激操作，提高可飞性和容错。

## 4. 典型控制架构（推荐落地）

### 4.1 两层闭环
1. 外环（目标生成）  
   鼠标误差 -> 期望角速度（pitch/yaw/roll rate command）。
2. 内环（速率控制）  
   角速度误差 -> 舵面输出（elevator/aileron/rudder）。

### 4.2 数学流程（简化）
1. 输入归一化  
   `ex = clamp((mouseX - cx) / sx, -1, 1)`  
   `ey = clamp((mouseY - cy) / sy, -1, 1)`
2. 曲线处理（指数/灵敏度）  
   `ex' = sign(ex) * |ex|^gammaX`  
   `ey' = sign(ey) * |ey|^gammaY`
3. 外环目标角速度  
   `yawRateCmd = kx * ex'`  
   `pitchRateCmd = ky * ey'`
4. 内环舵面控制（PD/PID）  
   `uPitch = KpP*(pitchRateCmd - q) + KdP*dErrPitch`  
   `uYaw   = KpY*(yawRateCmd - r)   + KdY*dErrYaw`
5. 保护与限幅  
   - 舵量限幅、舵量变化率限制  
   - 基于空速/攻角动态收缩最大指令角速度  
   - 低速下自动减小高风险俯仰指令

## 5. 关键手感组件
- 输入滤波：一阶低通抑制高频抖动，避免“舵面噪声”。
- 动态增益：随空速变化调整灵敏度，防止高速过敏和低速迟钝。
- 协调转弯：侧滑较大时自动方向舵补偿，降低“甩尾”。
- 自动配平/姿态保持：鼠标回中时给弱保持，减少持续修杆负担。
- 保护优先级：安全约束应覆盖玩家极端输入，避免直接进入不可恢复状态。

## 6. 参数起步建议（可用于首轮调参）
- 输入死区：`0.02 ~ 0.05`
- 指数曲线 `gamma`：`1.4 ~ 1.9`
- 目标角速度上限：  
  - `pitchRateMax`: `50 ~ 90 deg/s`  
  - `yawRateMax`: `30 ~ 60 deg/s`  
  - `rollRateMax`: `90 ~ 180 deg/s`
- 低通滤波系数（dt=50Hz示例）：`alpha = 0.15 ~ 0.35`
- 攻角软限制阈值：接近阈值后先“软削顶”，再硬限制。

## 7. 推荐调参顺序
1. 先固定保护阈值，调输入曲线和死区。
2. 再调外环角速度上限，确定整体“快慢感”。
3. 最后调内环 PID/PD，让机体跟随稳定且不过冲。
4. 尾段验证低速、大迎角、急转、俯冲拉起等极限工况。

## 8. 在本项目中的实现建议
- 优先将“鼠标输入”和“舵面输出”解耦，新增“目标指令层”。
- 在飞机/直升机控制 Tick 中插入外环与内环，保留开关便于 A/B 对比。
- 增加调试可视化：目标方向、当前机头、角速度误差、舵面输出、保护触发状态。
- 提供两套预设：
  - WT 风格：强稳定、强保护、较低操作门槛。
  - 模拟风格：弱稳定、弱保护、高机动上限。

## 9. 风险与注意事项
- 仅靠提高灵敏度无法得到“鼠标拖机头”体验，必须有闭环控制。
- 保护逻辑过强会造成“粘滞感”和“抢杆感”，需在可飞性与可控性间平衡。
- 多机型参数不宜完全共用，至少按机型类别（战斗机/攻击机/直升机）分组。

## 10. 结论
- 《战争雷霆》类鼠标飞控的核心是“目标驱动 + 双环闭环 + Instructor 保护”。
- 工程上最关键的是控制架构与保护策略，而不是单点参数。
- 建议先做可切换原型并配套可视化调参，再逐步固化到机型配置体系。

## 11. 本次需求收敛（2026-04-17）
- 鼠标指向定义：采用“虚拟杆目标点”作为鼠标指向位置，而不是操作系统绝对鼠标坐标。
- 启用视角限制：仅在 `thirdPersonView = 1`（第三人称后视）且玩家为飞机驾驶员时启用鼠标飞控。
- 渲染要求：启用鼠标飞控时渲染两个圆圈：
  - 机头指向位置渲染较小圆圈。
  - 虚拟杆目标点位置渲染稍大的圆圈。
- 配置开关：新增总开关（建议名 `MouseAimPlaneThirdPersonEnabled`），用于一键启停该功能。
- 扩展开关：可选新增显示开关（建议名 `MouseAimPlaneDrawCircles`），用于单独控制两个圆圈的显示。

## 12. 实施步骤与计划（仅 WT 风格）
- 风格冻结：本次仅实现 WT 风格鼠标飞控，不提供“模拟风格”分支。
- 阶段 1（配置层）：在 `MCH_Config` 增加 `MouseAimPlaneThirdPersonEnabled` 与 `MouseAimPlaneDrawCircles`，并写入配置说明。
- 阶段 2（启用条件）：在客户端飞行输入流程中增加启用判定：`飞机驾驶位 && thirdPersonView == 1 && 总开关开启`，不满足则走原有控制逻辑。
- 阶段 3（WT 控制映射）：将“虚拟杆目标点”映射为 WT 风格目标指令（优先俯仰/偏航，滚转做辅助），并加入限幅与平滑，避免突变。
- 阶段 4（双圆圈渲染）：新增/扩展渲染器，在启用鼠标飞控且显示开关开启时绘制：
  - 小圆圈：机头指向投影点。
  - 大圆圈：虚拟杆目标点投影位置。
- 阶段 5（事件注册与联调）：在客户端初始化阶段注册渲染事件，完成与现有 HUD、锁定框等叠加验证，避免冲突。
- 阶段 6（验收）：按场景验收
  - 第一人称：不启用鼠标飞控，不显示双圆圈。
  - 第三人称后视（`thirdPersonView = 1`）：启用鼠标飞控，显示双圆圈。
  - 第三人称前视（`thirdPersonView = 2`）：不启用鼠标飞控，不显示双圆圈。
  - 关闭总开关：所有视角均不启用、不显示。
